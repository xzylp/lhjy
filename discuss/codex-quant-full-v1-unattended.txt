# Quant full v1 unattended queue
# Source of truth:
# - discuss/quant-full-v1-complete.md
# - discuss/quant-full-v1-task-breakdown.md

先做基线核对，不要盲改。
必须阅读：
- discuss/quant-full-v1-complete.md
- discuss/quant-full-v1-task-breakdown.md
- src/ashare_system 下已实现的 sentiment/strategy/risk/contracts 代码
- tests 下与 quant 改造相关的测试

任务目标：
1. 确认 M0-M2 当前已完成到什么程度，列出“已完成 / 未完成 / 与方案有偏差”的清单。
2. 不重复实现已经完成并通过测试的部分。
3. 把核对结果写入 discuss/执行记录-codex.md，作为后续续跑基线。
4. 运行与当前已实现 quant 首批改造相关的测试，记录结果。

约束：
- 这一轮以审计和补缺口识别为主，除非发现明显缺失或兼容性问题，否则不要大改。
- 输出必须明确指出下一轮应该先做 M3 还是先补前序缺口。
---
继续上一会话，进入 M3。

必须以 discuss/quant-full-v1-complete.md 和 discuss/quant-full-v1-task-breakdown.md 为准，
目标是“让前排识别和个股股性画像成立”，优先完成最小可用闭环。

任务目标：
1. 扩展 contracts，补齐 StockBehaviorProfile、LeaderRankResult 等 M3 所需契约。
2. 实现 strategy/stock_profile.py 的最小可用版本。
3. 实现 strategy/leader_rank.py 的最小可用版本。
4. 把股性画像和前排相对排名接入现有 router / playbook / buy_decision 主链路，但保持兼容。
5. 新增或更新测试，验证：
   - 个股股性画像可构建
   - 龙头相对排名可输出
   - 路由或买入决策能利用前排信息
6. 把变更、设计取舍、待补点追加写入 discuss/执行记录-codex.md。

约束：
- 优先少改动、强兼容。
- 如果某些字段数据源暂时不足，允许给出保守默认值，但要在文档中写明。
- 完成后运行相关测试。
---
继续上一会话，进入 M4。

必须以 discuss/quant-full-v1-complete.md 和 discuss/quant-full-v1-task-breakdown.md 为准，
目标是“让快撤和战法化退出成立”，但保留旧有 sell_decision 底层回退。

任务目标：
1. 扩展 contracts，补齐 ExitSignal 等 M4 所需契约。
2. 实现 strategy/exit_engine.py 的最小规则版本，覆盖：
   - 开仓失败立撤
   - 炸板立撤
   - 板块退潮快撤
   - 时间止盈/止损
   - 旧 ATR 回退逻辑接管
3. 接入 sell_decision.py 或相关退出链路，优先保持旧逻辑仍可正常工作。
4. 新增或更新测试，验证：
   - 快撤信号成立
   - 没命中新规则时仍走旧回退
5. 把变更、退出优先级、占位实现说明追加写入 discuss/执行记录-codex.md。

约束：
- 不要把整个卖出链路推翻重写。
- 对缺少实时字段的场景，用保守兼容方案落地。
- 完成后运行相关测试。
---
继续上一会话，进入 M5。

必须以 discuss/quant-full-v1-complete.md 和 discuss/quant-full-v1-task-breakdown.md 为准，
目标是“OpenClaw 编排和讨论协议落地”，让 ashare 作为消息总线和主持人更可用。

任务目标：
1. 审视当前 discussion / openclaw / prompt 相关目录和实现。
2. 设计并实现最小可用的 dossier / packet / finalize 数据结构或辅助模块。
3. 调整相关 prompt、协议或归档逻辑，让 runtime / research / strategy / risk / audit 的输入输出结构更固定。
4. 新增或更新测试或 smoke 脚本，验证讨论协议最小链路可用。
5. 把协议结构、兼容策略、未完成项追加写入 discuss/执行记录-codex.md。

约束：
- 不改 main -> ashare -> 子代理 的大架构。
- 优先做结构固定和最小可验证链路，不追求一次把所有讨论能力铺满。
- 完成后运行相关测试或 smoke。
---
继续上一会话，进入 M6。

必须以 discuss/quant-full-v1-complete.md 和 discuss/quant-full-v1-task-breakdown.md 为准，
目标是“回测归因与学习闭环”，至少落可解释的最小框架。

任务目标：
1. 审视现有 backtest / learning / report 能力。
2. 设计并实现最小 attribution 结构，至少能按：
   - playbook
   - regime
   - exit_reason
   做结果聚合。
3. 补最小 trade review / learning 闭环接口或数据对象，使后续可继续扩展。
4. 新增或更新测试，验证归因聚合链路最小可用。
5. 把本轮结果、限制、下一版建议追加写入 discuss/执行记录-codex.md。

约束：
- 先落框架和对象，不强求一次做全量高精度回测。
- 与已有代码结构保持一致，不另起平行系统。
- 完成后运行相关测试。
---
继续上一会话，做最终收口。

任务目标：
1. 回顾 M3-M6 改动，确认没有明显重复、冲突和破坏。
2. 统一补齐必要的文档和注释。
3. 运行一轮与你本次改动最相关的测试集合。
4. 在 discuss/执行记录-codex.md 输出最终总结：
   - 完成项
   - 未完成项
   - 风险项
   - 下一批建议任务

约束：
- 这一轮以收口和验证为主，不做无边界扩写。
